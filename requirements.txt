# BT Monitor — runtime dependencies (pinned for reproducibility, #14).
# Test/dev tooling lives in requirements-dev.txt.
#
# Browser driver: patchright only (stealth Playwright fork). Plain `playwright`
# was removed — nothing imports it. `anthropic` was removed — unused since the
# Claude-vision check was dropped.

# --- web / browser automation ---
patchright==1.60.1          # drives Chromium for the deep scan (stealth fork)
httpx==0.28.1               # fast async probes (live watcher + link checks)
beautifulsoup4==4.14.3      # sitemap / HTML parsing
lxml==6.1.1                 # bs4 parser backend

# --- document scanning ---
pypdf==6.12.2               # PDF text extraction (doc-leak scanner)
python-docx==1.2.0          # DOCX text extraction

# --- web app / dashboard ---
fastapi==0.136.3
uvicorn==0.49.0
jinja2==3.1.6
python-multipart==0.0.32    # form parsing for POST routes
flask==3.1.3                # test_fixture/server.py only

# --- persistence ---
SQLAlchemy==2.0.50
# For PostgreSQL in production, also install:  psycopg[binary]>=3.2
# then set BT_MONITOR_DB_URL=postgresql+psycopg://user:pw@host:5432/bt_monitor

# --- exports ---
openpyxl==3.1.5             # Excel export
reportlab==4.5.1            # PDF audit export

# --- platform ---
tzdata==2026.2 ; sys_platform == 'win32'   # Europe/Bucharest tz on Windows
